# Backend URL (no trailing slash). Used for API calls.
VITE_BACKEND_URL=http://localhost:4000
# Dashboard URL (no trailing slash). Used for "Login as Admin" redirect.
VITE_DASHBOARD_URL=http://localhost:5174
